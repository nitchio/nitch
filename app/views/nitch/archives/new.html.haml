.row
  .span8
    %h2.page-header= "New #{current_nitch.settings.archive_name}"
    = form_for @archive, url: archives_url do |f|
      = render partial: 'nitch/archives/form', locals: { f: f }
      .form-actions
        = f.submit 'Save', class: 'btn btn-primary'